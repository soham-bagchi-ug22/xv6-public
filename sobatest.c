#include "types.h"
#include "user.h"

// NOT MY TEST CASE, this will be rewritten

int main (int argc, char *argv[]){
	int a = 0;
	float b = 1;

	
	while (a < 30){
		b = b + 100;
		a++;
	}
	printf(1, "here %d\n", a);	
	exit();
}